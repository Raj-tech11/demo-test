import Button from "../../components/Button/Button";

const Dashboard = () => {
  return (
    <div>
      Dashboard page
      <Button title="Cancel" />
    </div>
  );
};

export default Dashboard;
